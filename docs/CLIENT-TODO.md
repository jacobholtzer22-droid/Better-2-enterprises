# CLIENT-TODO — Unconfirmed facts (do not publish until confirmed)

Every item below has a copy slot built and waiting. Nothing here appears on
the site until Steve confirms it — the page ships with the element omitted
rather than guessed.

| # | Item | Why it matters | Where it goes once confirmed |
|---|---|---|---|
| 1 | **License status + number, bonding, insurance certificate** | Public records show conflicting license status and a May 2026 expiration. A "Licensed & Insured" claim without a current certificate is a liability. | Trust strip on home + about page + footer. Five-minute fill once a current certificate is in hand: add a `credentials` block to `site.config.ts` `identity`. |
| 2 | **Street address public?** (7305 Dodge, Montrose) | Many contractors run from a home/yard and prefer service-area-only. Currently the site presents as service-area-only. | Flip `identity.address.public` to `true` in `site.config.ts` → footer NAP + contact page + `address` property in LocalBusiness schema. |
| 3 | **Business hours** | Get from the Google Business Profile — use exactly what the GBP shows. Currently omitted everywhere (no `openingHoursSpecification` in schema). | `identity.hours` in `site.config.ts` → contact page + schema. |
| 4 | **Email address** | No confirmed email. Form + phone are the only contact channels. | `identity.email` → contact page + footer + schema `email`. |
| 5 | ~~**Named school districts / commercial clients**~~ **RESOLVED Sept 2026** | Steve provided a project sheet PDF naming City of Flint, Mott Community College, Clio Area Schools, Charter Township of Mount Morris, Yale Public Schools, and City of Midland. Published in the project record with full details. | Done — `projects.publicWorks` in `site.config.ts`. |
| 6 | **Crew size, equipment, employee count** | Not stated anywhere; not confirmed. | About page, if Steve wants it. |
| 7 | **Any dollar figures / price ranges** | Never published without client sign-off. FAQ answers explain cost drivers and invite a quote instead. | Residential FAQ, if ever confirmed. |
| 8 | **Response-time expectation** ("we return calls within X") | Only if Steve commits to one. | Contact page intro. |
| 9 | **BBB profile URL + BuildZoom profile URL** | Needed for `sameAs` in LocalBusiness schema (real URLs only). | `identity.bbb.profileUrl` / `identity.buildZoomUrl` in `site.config.ts` → schema `sameAs`. |
| 10 | **Google Business Profile URL** | Jacob supplies after launch; redeploy adds it to `sameAs`. | `identity.googleBusinessProfileUrl` → schema. |
| 11 | **Final production domain** | `site.url` reads the `PRODUCTION_URL` constant at the top of `site.config.ts`, currently the placeholder `https://www.PUT_DOMAIN_HERE.com`. Canonicals, sitemap, OG URLs, and the LocalBusiness schema `@id` all derive from it. | One-line edit: `PRODUCTION_URL` constant, `site.config.ts` (top of file). |
| 12 | **GBP phone must match (810) 493-3521** | Site phone changed Sept 2026 from (810) 397-5000 to (810) 493-3521 per client. NAP consistency: the Google Business Profile must be updated to show the SAME number character for character, or local rankings suffer. Verify the GBP before/at launch. | GBP edit (Jacob); site already updated sitewide. |
| 13 | **Real job-site photos** | The site currently uses **licensed Pexels stock photography** (Jacob-authorized interim measure — full inventory and swap plan in `docs/PHOTOS.md`). Stock never appears on `/projects` and is never captioned as Better 2's work, but real photos are strictly better everywhere. Use only photos the business uploaded/owns — NOT the 3 customer photos Michael Norris posted to the GBP. Also decide with Steve whether he wants a visible "photos are illustrative" note anywhere. | Follow the swap steps in `docs/PHOTOS.md`; update the `images` block in `site.config.ts`. |
| 14 | **Verified `businessSlug` for the quote form** | A wrong slug = HTTP 200 with no DB write; leads vanish silently. Currently `PENDING_SLUG_VERIFICATION` — production build is blocked on it. | `form.businessSlug` in `site.config.ts` (Phase 4 gate — Jacob verifies against the live Neon row). |
| 15 | **USDOT number on commercial/municipal pages?** | Better 2 Enterprises holds **USDOT 2792668** as a registered Michigan motor carrier — a real credibility signal (they run their own trucks rather than subbing haul). Ask Steve whether he wants it published. Do NOT publish fleet size, driver counts, or any safety/inspection data. | Commercial + municipal pages, small credential line; add a `usdot` field to `identity` in `site.config.ts`. |
| 16 | **Confirm the service list with Steve** | Items beyond the permit record came from the build brief's service lists, not from Steve directly. Confirm he actually offers each before launch so the site doesn't generate leads he turns down. Most important to confirm: curb & gutter, loading docks & approaches, parking area concrete, ADA ramps & detectable warning surfaces, equipment/dumpster pads (commercial); municipal sidewalk & curb programs, public building approaches (municipal); porches & steps, footings (residential). Note: stamped/decorative, staining, polished floors, mudjacking/leveling, retaining walls, and excavation are deliberately NOT on the site — do not add without confirmation. | Trim `services.*.items` in `site.config.ts` if Steve declines any. |
